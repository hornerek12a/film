import styled from "styled-components";

export const Wrapper = styled.div`
display: flex;
  border-radius: 12px;
  overflow: hidden;
  position: relative;
`
export const Header = styled.h2`
font-weight: 900;
color: calc();
`